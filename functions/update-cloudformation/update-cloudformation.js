exports.handler = function(event, context, callback) {
  // aws sdk.
  // cloudformation
  // update-stack
  // use-previous-template
  // template-url
  // udpdate some metadata
//  this will bne triggered when the s3://bucket-with the other lambdas gets uploaded to. 
// it will trigger an update on cloudformation
// this one should be in its own bucket. 
}
